# Scripts

Ten katalog zawiera proste skrypty do codziennej obsługi projektu.

- `start.sh` - uruchamia kontenery
- `stop.sh` - zatrzymuje kontenery
- `status.sh` - pokazuje stan usług
- `logs.sh` - pokazuje ostatnie logi z Docker Compose
