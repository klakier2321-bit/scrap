{
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": {
          "type": "grafana",
          "uid": "-- Grafana --"
        },
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "none" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 4, "x": 0, "y": 0 },
      "id": 1,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto" },
      "targets": [{ "expr": "sum(crypto_ai_active_runs)", "refId": "A" }],
      "title": "Active Runs",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "none" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 4, "x": 4, "y": 0 },
      "id": 2,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto" },
      "targets": [{ "expr": "sum(increase(crypto_ai_agent_runs_total{status=\"completed\"}[24h]))", "refId": "A" }],
      "title": "Completed Runs 24h",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "none" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 4, "x": 8, "y": 0 },
      "id": 3,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto" },
      "targets": [{ "expr": "sum(increase(crypto_ai_agent_runs_total{status=\"failed\"}[24h]))", "refId": "A" }],
      "title": "Failed Runs 24h",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 9, "w": 12, "x": 0, "y": 8 },
      "id": 4,
      "targets": [
        {
          "expr": "sum by (agent_name) (increase(crypto_ai_agent_runs_total{status=\"completed\"}[1h]))",
          "legendFormat": "{{agent_name}}",
          "refId": "A"
        }
      ],
      "title": "Completed Runs by Agent (1h)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 9, "w": 12, "x": 0, "y": 17 },
      "id": 5,
      "targets": [
        {
          "expr": "sum by (agent_name, model) (increase(crypto_ai_llm_calls_total[1h]))",
          "legendFormat": "{{agent_name}} / {{model}}",
          "refId": "A"
        }
      ],
      "title": "LLM Calls by Agent and Model (1h)",
      "type": "timeseries"
    }
  ],
  "refresh": "10s",
  "schemaVersion": 39,
  "tags": ["crypto-system", "agents"],
  "templating": { "list": [] },
  "time": { "from": "now-24h", "to": "now" },
  "title": "Agent Overview",
  "uid": "crypto-agent-overview",
  "version": 1
}
