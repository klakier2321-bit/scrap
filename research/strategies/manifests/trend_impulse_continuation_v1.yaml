strategy_id: trend_impulse_continuation_v1
display_name: Trend Impulse Continuation
version: "1.0.0"
strategy_family: trend_continuation
risk_profile: balanced
execution_style: confirmation
archetype: continuation on fresh impulse
status: proposed
supported_regimes: [trend_up, trend_down]
supported_market_states: [trend]
supported_market_phases: [expansion, mature_trend]
supported_volatility_phases: [expanding, cooling]
supported_biases: [long, short]
supported_event_contexts: []
minimum_data_trust: limited_trust
disallowed_conditions: [no_trade_zone, high_noise_environment, post_shock_cooldown]
required_data_inputs: [regime_report, feature_snapshot, market_consensus]
optional_data_inputs: [derivatives_state_global]
signal_contract:
  required_fields: [strategy_id, signal_id, direction, setup_quality, entry_type, invalidation, exit_logic_template, aggressiveness_tag, regime_alignment]
parameter_schema:
  min_setup_quality: {type: float, min: 0.60, max: 0.90, step: 0.01, default: 0.74}
entry_semantics:
  requires_confirmation: true
  supports_limit_entry: false
  supports_market_entry: true
exit_semantics:
  structure_exit: true
  trailing_exit: true
telemetry_requirements: [setup_detected, signal_built, blocked_by_risk, entry, exit]
backtest_priority: high
steward_agent_role: Trend Impulse Steward
owner_team: strategy_stewards
dependencies: [regime_engine, risk_engine]
implementation_status: planned
notes:
  - Planned after pullback continuation proves stable.
