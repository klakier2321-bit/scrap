strategy_id: transition_wait_filter_v1
display_name: Transition Wait Filter
version: "1.0.0"
strategy_family: defense_only
risk_profile: defensive
execution_style: confirmation
archetype: explicit wait or no-trade filter
status: proposed
supported_regimes: [low_vol, range, stress_panic]
supported_market_states: [transition, range]
supported_market_phases: [transition, compression]
supported_volatility_phases: [compression, cooling]
supported_biases: [neutral]
supported_event_contexts: []
minimum_data_trust: low_trust
disallowed_conditions: []
required_data_inputs: [regime_report, risk_decision]
optional_data_inputs: []
signal_contract:
  required_fields: [strategy_id, signal_id, direction, setup_quality, entry_type, invalidation, exit_logic_template, aggressiveness_tag, regime_alignment]
parameter_schema:
  wait_state_threshold: {type: float, min: 0.20, max: 0.80, step: 0.05, default: 0.50}
entry_semantics:
  requires_confirmation: false
exit_semantics:
  time_stop: false
telemetry_requirements: [setup_detected, blocked_by_risk]
backtest_priority: low
steward_agent_role: Transition Filter Steward
owner_team: strategy_stewards
dependencies: [regime_engine, risk_engine]
implementation_status: planned
notes:
  - Explicit no-trade and waiting-mode strategy.
