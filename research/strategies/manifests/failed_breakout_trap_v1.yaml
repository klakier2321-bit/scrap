strategy_id: failed_breakout_trap_v1
display_name: Failed Breakout Trap
version: "1.0.0"
strategy_family: mean_reversion
risk_profile: balanced
execution_style: fade
archetype: trap or failed expansion reversion
status: proposed
supported_regimes: [range, high_vol]
supported_market_states: [range, transition]
supported_market_phases: [expansion, transition]
supported_volatility_phases: [expanding, cooling]
supported_biases: [long, short]
supported_event_contexts: []
minimum_data_trust: limited_trust
disallowed_conditions: [no_trade_zone, high_noise_environment]
required_data_inputs: [regime_report, feature_snapshot, market_consensus]
optional_data_inputs: [derivatives_state_global]
signal_contract:
  required_fields: [strategy_id, signal_id, direction, setup_quality, entry_type, invalidation, exit_logic_template, aggressiveness_tag, regime_alignment]
parameter_schema:
  rejection_strength_min: {type: float, min: 0.10, max: 1.0, step: 0.05, default: 0.40}
entry_semantics:
  requires_confirmation: true
  supports_limit_entry: true
  supports_market_entry: true
exit_semantics:
  structure_exit: true
telemetry_requirements: [setup_detected, signal_built, blocked_by_risk, entry, exit]
backtest_priority: medium
steward_agent_role: Trap Fade Steward
owner_team: strategy_stewards
dependencies: [regime_engine, risk_engine]
implementation_status: planned
notes:
  - Designed for post-breakout rejection, not for normal trend continuation.
