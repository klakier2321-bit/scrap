strategy_id: defense_only_v1
display_name: Defense Only
version: "1.0.0"
strategy_family: defense_only
risk_profile: defensive
execution_style: confirmation
archetype: runtime defense and no-trade advisory
status: experimental
supported_regimes: [low_vol, stress_panic, range]
supported_market_states: [transition, range]
supported_market_phases: [transition, compression]
supported_volatility_phases: [compression, cooling, extreme]
supported_biases: [neutral, long, short]
supported_event_contexts: []
minimum_data_trust: broken
disallowed_conditions: []
required_data_inputs: [regime_report, risk_decision]
optional_data_inputs: [derivatives_state_global]
signal_contract:
  required_fields: [strategy_id, signal_id, direction, setup_quality, entry_type, invalidation, exit_logic_template, aggressiveness_tag, regime_alignment]
parameter_schema:
  advisory_mode: {type: string, default: wait}
entry_semantics:
  requires_confirmation: false
exit_semantics:
  time_stop: false
telemetry_requirements: [setup_detected, blocked_by_risk, blocked_by_execution]
backtest_priority: high
steward_agent_role: Defense Runtime Steward
owner_team: strategy_stewards
dependencies: [regime_engine, risk_engine, execution_guard]
implementation_status: implemented
notes:
  - Advisory no-trade strategy for poor market quality or explicit defensive runtime states.
