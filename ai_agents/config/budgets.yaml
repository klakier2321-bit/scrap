global:
  daily_usd: 4.0
  per_run_usd: 0.35
  require_approval_for_strong_model: true

agents:
  review_agent:
    daily_usd: 0.75
    per_run_usd: 0.1
    max_iterations: 3
    max_retry_limit: 1
    require_approval_for_strong_model: false

  system_lead_agent:
    daily_usd: 1.2
    per_run_usd: 0.2
    max_iterations: 3
    max_retry_limit: 1
    require_approval_for_strong_model: true

  architecture_agent:
    daily_usd: 0.2
    per_run_usd: 0.08
    max_iterations: 2
    max_retry_limit: 1
    require_approval_for_strong_model: true

  monitoring_agent:
    daily_usd: 0.2
    per_run_usd: 0.08
    max_iterations: 2
    max_retry_limit: 1
    require_approval_for_strong_model: false

  control_layer_agent:
    daily_usd: 0.25
    per_run_usd: 0.1
    max_iterations: 3
    max_retry_limit: 1
    require_approval_for_strong_model: false

  strategy_agent:
    daily_usd: 1.25
    per_run_usd: 0.25
    max_iterations: 4
    max_retry_limit: 1
    require_approval_for_strong_model: false

  alpha_research_agent:
    daily_usd: 0.5
    per_run_usd: 0.12
    max_iterations: 3
    max_retry_limit: 1
    require_approval_for_strong_model: false

  feature_engineering_agent:
    daily_usd: 0.75
    per_run_usd: 0.18
    max_iterations: 3
    max_retry_limit: 1
    require_approval_for_strong_model: false

  regime_model_agent:
    daily_usd: 0.2
    per_run_usd: 0.08
    max_iterations: 3
    max_retry_limit: 1
    require_approval_for_strong_model: false

  risk_research_agent:
    daily_usd: 0.75
    per_run_usd: 0.18
    max_iterations: 3
    max_retry_limit: 1
    require_approval_for_strong_model: false

  experiment_evaluation_agent:
    daily_usd: 0.25
    per_run_usd: 0.1
    max_iterations: 3
    max_retry_limit: 1
    require_approval_for_strong_model: false

  integration_agent:
    daily_usd: 0.2
    per_run_usd: 0.1
    max_iterations: 3
    max_retry_limit: 1
    require_approval_for_strong_model: false

  api_agent:
    daily_usd: 0.15
    per_run_usd: 0.08
    max_iterations: 2
    max_retry_limit: 1
    require_approval_for_strong_model: false

  gui_agent:
    daily_usd: 0.15
    per_run_usd: 0.08
    max_iterations: 2
    max_retry_limit: 1
    require_approval_for_strong_model: false
